/*

DrawFormatString(0, 0, GetColor(255,255,255), "êîíl1:%d", );
DrawFormatString(0, 0, GetColor(255,255,255), "êîíl1:%d,êîíl2:%d"
	, , );
clsDx();
printfDx("%d ",);

Enemy(
1	float X, float Y,
2	int ImageType, eBulletMode Mode,bool IsBossMove,
3	float Speed, float Speedrate,
4	double Angle, double Carbdegree, double Anglerate,
5	int BulletColor, double BulletAngle, int ShotSpeed, int AimPlayerNum,
6	int StopFrame = 0, int StartShotFrame = 0, int EndShotFrame = INF,
7	double StopCarbAngle=INF
)

Enemy(
1	float X, float Y, float GoalX, float GoalY,
2	int ImageType, eBulletMode Mode,bool IsBossMove,
3	float Speed, float Speedrate,
4	double Angle, double Carbdegree, double Anglerate,
5	int BulletColor, double BulletAngle, int ShotSpeed, int AimPlayerNum,
6	int StopFrame = 0, int StartShotFrame = 0, int EndShotFrame = INF,
7	double StopCarbAngle=INF
)

Boss(
int ImageType, eBulletMode Mode,
int BulletColor, int ShotSpeed,
int MoveFrame, int StopFrame, int SpecialShotLife)

Bullet(
1	float X, float Y,
2	int ImageType, int Color, float Size,
3	float Speed, float Speedrate,
4	double Angle, double Carbdegree, double Anglerate,
5	int StopFrame = 0,double StopCarbAngle=INF,int ReflectCount = 0
)

Bullet(
1	float X, float Y, float GoalX, float GoalY,
2	int ImageType, int Color, float Size,
3	float Speed, float Speedrate,
4	double Angle, double Carbdegree, double Anglerate,
5	int StopFrame = 0,double StopCarbAngle=INF,int ReflectCount = 0
)
*/


//-----------------------------------------------------
//íËêî



//-----------------------------------------------------